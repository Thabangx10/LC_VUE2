import { createStore } from 'vuex'

export default createStore({
  state: {
    // Check the state of our admin data
  },
  getters: {
  },
  mutations: {
    // Provide updates required to change the state NULL value
  },
  actions: {
    // Apply some changes to our static state using the mutable properties apply above
  },
  modules: {
  }
})
